<div class="col-md-1">
   <h5 class="bg-light">ジャンル編集</h5>
</div>
<%= form_with model: @genre do |f| %>
<div class="container">
  <div class="row">
    <div class="col-md-4">
      <label for="field">
        <%= f.label:ジャンル名　%>
        <%= f.text_field:name %>
        <%= f.submit '変更を保存',class:'btn btn-sm btn-success' %>
      </label>
    </div>
  </div>
</div>