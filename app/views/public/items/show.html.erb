
<%= form_with model: @cart_item,url: public_cart_items_path do |f| %>
<div class="container">
    <div class ="row">
        <div clas="col-md-4">
             <%= image_tag @item.get_image,size:"300x300" %>
        </div>
        <div class="col-md-8">
            <h4><strong><%= @item.name %></strong></h4>

            <p><%= @item.introduction %></p>


            <p><%= @item.price %></p>
            <%= f.hidden_field :amount,:value => "数量を選択してください" %>
            <%= f.select:amount,*[1..5] %>

            <%= f.submit 'カートに入れる',class:'btn btn-sm btn-success' %>
        </div>

    </div>

</div>
<% end %>


