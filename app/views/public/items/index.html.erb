<h4>商品一覧</h4>

  <div class="container">
  <% @items.each do |item| %>

   <div>
     <%= link_to public_path(item.id) do %>
      <%= image_tag item.get_image,size:"200x300" %>
     <% end %>
   </div>

    <p><%= item.name %></p>
    <p>¥<%= item.with_tax_price %></p>
  </div>
  <% end %>